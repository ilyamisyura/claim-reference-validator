# Embedding Service Configuration

# Model settings
EMBEDDING_DEFAULT_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_MODEL_CACHE_DIR=./models

# API settings
EMBEDDING_HOST=0.0.0.0
EMBEDDING_PORT=8001

# Performance
EMBEDDING_BATCH_SIZE=32
EMBEDDING_NORMALIZE_EMBEDDINGS=true

# CORS (comma-separated list)
EMBEDDING_CORS_ORIGINS=*
