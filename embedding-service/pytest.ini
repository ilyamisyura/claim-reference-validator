[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')

# Essential options only - add others as needed per run
addopts =
    --strict-markers

# Test discovery
minversion = 9.0
